# V2 冒烟验证记录

日期：2026-09-08。使用独立临时库和合成视频，不修改用户原 Review 或客户端配置。

## 结果

- Swift 测试：36 项，0 失败、0 跳过；显式排除真实客户端安装测试。日志：`/tmp/ui-review-v2-smoke.log`。
- 新增 `MotionSmokeTests`：通过实际 Store 导入当前视频、导入参考（确认前不关联）、设置参考、添加时间点及区域评论、保存重开，随后导出并读取当前/参考证据文件。此项通过程序入口执行，不代表真实鼠标框选已验收。
- 播放验证：快速连续定位保留最终帧，双播放器实际推进，暂停生效。
- 媒体验证：30 fps、VFR、旋转、60 fps 与 SDR HEVC 合成样本，精确取帧、区域图及导出通过。
- 打包 MCP：旧截图 17 次请求、动画 11 次请求，共 28 次通过，读取前后库内容一致。动画测试首次在沙箱内返回 Cannot Decode，允许访问系统媒体服务后复验通过。
- 开发预览包严格签名验证通过；使用另一份独立库启动并观察 8 秒，进程持续运行未提前退出，随后仅关闭本次测试进程。此检查不证明窗口内容已完成视觉验收。

## 限制

本轮未通过桌面自动化执行实际拖动、框选调整、Esc、深色主题或窄窗口检查；此前桌面工具存在长时间无响应。真实手机录屏与 macOS 14 设备验证仍未完成。自动化流程通过不等同于全部 PRD 视觉验收或正式发布验收。

## 2026-09-09 参考视频播放闪退修复

用户设备崩溃报告 `UIReview-2026-09-09-135731.ips` 显示已安装应用在播放画面创建时，因 `_AVKit_SwiftUI` 类元数据初始化触发 Swift fatalError/SIGABRT。对齐页面和双视频工作区使用的 SwiftUI VideoPlayer 已统一替换为 NSViewRepresentable + AVPlayerLayer，沿用 MotionSession 的播放控制与时间逻辑。

新增显示组件真实 NSHostingView 加载、播放器替换与解绑验证，并在双播放器实际推进测试中加载两侧显示组件。6 项相关测试通过，0 失败、0 跳过；日志 `/tmp/ui-review-reference-playback-tests.log`。这补足了此前仅测试播放状态、没有创建播放界面的缺口；未宣称完成实际鼠标全流程验收。

2.0.1（build 3）已打包并通过严格签名验证。已确认安装版退出，备份旧应用到工作区 build 目录，替换 `/Applications/UI Review.app` 并核对二进制一致；用户 Review 数据未修改。

## 2026-09-09 动画快捷删除与 2.0.2

Command+Delete 现在删除当前选中的截图或动画素材，沿用现有删除及撤销机制；输入框编辑不触发素材删除，长按重复事件不会继续删除下一素材。8 项相关快捷键与 Store 回归测试通过，0 失败、0 跳过，日志 `/tmp/ui-review-delete-tests.log`。2.0.2（build 4）已构建并验证签名，安装版已更新，旧应用已备份；正在运行的应用需退出重开才能加载新代码。
